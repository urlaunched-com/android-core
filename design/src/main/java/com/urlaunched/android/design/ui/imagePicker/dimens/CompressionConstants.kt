package com.urlaunched.android.design.ui.imagepicker.dimens

internal object CompressionConstants {
    const val DEFAULT_MAX_SIZE_BYTES: Long = 15 * 1024 * 1024
    const val DEFAULT_TARGET_SIZE_BYTES: Long = 5 * 1000 * 1000
    const val DEFAULT_MIN_HEIGHT = 1080
    const val DEFAULT_MIN_WIDTH = 1920
}